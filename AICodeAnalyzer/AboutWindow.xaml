<Window x:Class="AICodeAnalyzer.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About AI Code Analyzer" Height="300" Width="400" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="AI Code Analyzer" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" Text="Version 1.0" 
                   FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="0,0,0,20">
            AI Code Analyzer lets you scan your entire code base and analyze it with different AI models.
            <LineBreak/><LineBreak/>
            Easily review code quality, identify bugs, detect security vulnerabilities, 
            and get improvement suggestions for your projects.
            <LineBreak/><LineBreak/>
            Supports multiple AI providers including Claude, ChatGPT, DeepSeek, Grok, and Gemini.
        </TextBlock>
        
        <Button Grid.Row="3" Content="OK" Width="80" Height="30" 
                HorizontalAlignment="Center" Click="BtnOk_Click"/>
    </Grid>
</Window>